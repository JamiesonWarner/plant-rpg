function Grid() {
  var RANGE = 50;
  var CELL_SIZE = 5;

  var graphics = game.add.graphics(0, 0);
  graphics.lineStyle(10, 0xffd900, 1);
  for (var i = -RANGE; i < RANGE.length; i++) {
    RANGE[i]
  };
}